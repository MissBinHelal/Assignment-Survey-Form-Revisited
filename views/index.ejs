<html>
<head>
    <title>Sockets</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script type="text/javascript" src="/socket.io/socket.io.js"></script>
    <script type ="text/javascript">
        $(document).ready(function (){
            
            var socket = io(); //1
        $('#survey').submit(function (e){
            e.preventDefault()

            var dic={name:$("#name").val(),location:$("#location").val(),languge:$('#languge').val(),comment:$('#comment').val()}
            socket.emit('submit',dic)
        })
    
          socket.on('send', function (data) { //4
        console.log(data.name);
        
                $('p').text("the submited information is "+data.msg)
            });
         })
    </script>
</head>
<body>
    <h1>Fun with sockets</h1> 

<p> </p>

<form id="survey">
    <div class="form-group col-md-4">
        <label for="uname">Your name</label>
        <input type="text" class="form-control" id="name" placeholder="Name">
    </div>

    <div class="form-group col-md-4">
        <label for="State">Dojo Location </label>
        <select id="location" class="form-control">
            <option selected="">Mauntain View</option>
            <option>Saudi Arabia</option>
            <option>United State</option>

        </select>
    </div>
    <div class="form-group col-md-4">
        <label for="Languge">Your favorite programming</label>
        <select id="languge" class="form-control">
            <option value="java" selected="">java</option>
            <option value="js">Java Script</option>
            <option value="python">Python</option>
        </select>
    </div>
    <div class="form-group col-md-4">
        <label for="Textarea1">Comment (optional) </label>
        <textarea class="form-control" id="comment" rows="3">comment</textarea><br>
        <button type="submit" class="btn btn-primary">Submit</button>

    </div>
</form>
</body>
</html>
